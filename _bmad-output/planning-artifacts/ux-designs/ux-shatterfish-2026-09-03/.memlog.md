---
topic: Shatterfish overlay UX (Panel, controls, states) within SPD's native toolkit
updated: 2026-09-03T18:28
---

- (event) Create intent (session 9, after PRD validation). Source scan candidates: prd.md section 4.6 (FR-37 to FR-47) and Glossary; research.md section 7 (Noosa building blocks, sizes, threading, hotkeys); docs/BOOTSTRAP-PROMPT.md section 5 (panel, controls, interjection, oracle). Stakes: one-user v1 developer tool, public repository, native toolkit mandated (non-negotiable 6)
- (decision) Product owner: the Panel is a visibly separate instrument inside the game (same toolkit, distinct engine-room tone: monospace numbers, terser, denser), not a lookalike of the game's own windows
- (decision) Product owner: the entire idea is an automated TAS; the human must be able to go step by step with the bot, not just watch. Run control is three modes: Next Step (one Action per press), Human play speed (paced so a person can follow), Fast as it can (uncapped). This replaces the PRD's turns-per-second cap wording in FR-39
- (assumption) Working mode fast path (owner did not pick; consistent with sessions 7 and 8). Small-window behavior not answered: assumed the Panel collapses to a one-line mode strip with a toggle when the free column is unavailable, and never overlays the map
- (event) Sources confirmed by default: PRD 4.6 and Glossary, research section 7, bootstrap section 5; extracted from the session context (already read this session)
- (decision) Spines drafted (fast path): DESIGN.md as an instrument-layer delta on the native toolkit (tint tokens only, one font at the game's sizes, nine-patch shapes, numbers in columns, red reserved for Danger and Oracle); EXPERIENCE.md with IA (Panel, Mode strip, Explain expansion, Map highlights, Key bindings, Launcher, v2 Replay scrubber and Coach), an invented Stepping model section (hero turn as the step; Next Step / Human play speed / Fast as it can), Interjection semantics, Oracle marking, states incl. THINKING, hero busy, no valid action, run over, save/resume, collapsed
- (assumption) Panel width target 100 UI px, minimum 72, collapse to Mode strip below; default keys F6 Next Step, F7 cycle speed, F8 Pause/Resume, F9 Take over/Hand back, F10 Explain, F11 toggle Panel; Human play speed default 1 turn/s, range 0.5-5 s; decision log keeps 200 lines on screen; Belief summary shows top three items
- (decision) Spines drafted (fast path): DESIGN.md as an instrument-layer delta on the native toolkit (tint tokens only, one font at the game's sizes, nine-patch shapes, numbers in columns, red reserved for Danger and Oracle); EXPERIENCE.md with IA (Panel, Mode strip, Explain expansion, Map highlights, Key bindings, Launcher, v2 Replay scrubber and Coach), an invented Stepping model section (hero turn as the step; Next Step / Human play speed / Fast as it can), Interjection semantics, Oracle marking, states incl. THINKING, hero busy, no valid action, run over, save/resume, collapsed
- (assumption) Panel width target 100 UI px, minimum 72, collapse to Mode strip below; default keys F6 Next Step, F7 cycle speed, F8 Pause/Resume, F9 Take over/Hand back, F10 Explain, F11 toggle Panel; Human play speed default 1 turn/s, range 0.5-5 s; decision log keeps 200 lines on screen; Belief summary shows top three items
- (decision) Hand back always lands in PAUSED (never RUNNING) so the human sees the next Decision before it acts; Next Step in RUNNING pauses first; three consecutive no-valid-action turns force PAUSED
- (event) Reviewer gate (rubric walker, run by delegation): 0 critical, 4 high, 11 medium, 15 low; review-rubric.md. Reconciliation against PRD 4.6, bootstrap 5, research 7: reconcile-prd-overlay.md
- (change) Spines updated: unit of stepping is the Input wait (Prompts included); launch state PAUSED + Next Step committed; enablement matrix per Mode; Run N restored with a count stepper; Human play speed interval stepper shown on the Mode strip; Pause-on conditions (v2) live in an Overlay section of the game's settings screen; PAUSED ignores hero input; Brain error state; unsupported human input logged; Panel overlays the dungeon edge translucently with the camera offset, never the HUD; goal-line token = title; stepper component; oracle enemies on the map in oracle color; coach may speak one sentence; hotkey buttons-only fallback stated
- (event) Key-screen mocks rendered to .working/ and promoted to mockups/: key-panel-paused.html (Panel in PAUSED at Flow 1 step 3 plus Explain expansion), key-panel-states.html (RUNNING, HUMAN, THINKING, collapsed, Oracle). Mock coverage: Panel, Mode strip, Explain expansion, Run N and pace settings, Map highlights mocked; Key bindings, Launcher, Replay scrubber (v2), Coach (v2), Pause-on settings (v2) spine-only by choice (no layout of their own)
- (change) Geometry from the mock: at interface scale 3 a 1280x720 window is 427x240 UI px; the column between menu pane and inventory pane is ~126 px tall; 100 px width wraps every line. Layout rule replaced: Panel left of the inventory pane's column, between status pane and toolbar; width target 200 (min 160); log flexes to remaining height (min 3 lines); collapse when uncovered map < 200 or view < 200 UI px; scale 3 at 1280x720 collapses. depth -> floor in the Voice table and Flow 3. Mock links inlined in both spines; spine wins on conflict. PRD FR-38 wording aligned
- (change) Editorial polish applied (structure + prose lenses): placement rule canonical in DESIGN Layout; PAUSED-input rule canonical in State Patterns; Hand back duplicate cut; THINKING and stepper restated once; Interaction Primitives as a Control/Behavior/Default key table with the F6-F11 assumption kept once; column schemas unified (Where); hero turn -> Input wait as the stepping unit; depth -> Floor; Tapping -> Clicking; long sentences split; minor fixes
- (decision) Resume from save starts PAUSED with speed mode Next Step regardless of the previous Mode and speed mode (stepper values are session-only); reason: the human should read a fresh Decision before the bot acts
- (decision) Oracle mode shows true identities in the Belief summary with the oracle prefix AND outlines unseen enemies on the map in the oracle color; Oracle marking is canonical; reason: matches the mocks and DESIGN Map highlight; the border and label still gate it
- (event) Spines finalized: DESIGN.md and EXPERIENCE.md status final; mockups/ holds two key screens linked inline
