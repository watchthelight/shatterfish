---
kind: citation-check
report: research.md
checked: 2026-09-03
method: every load-bearing number, date, version or does/does-not sentence in the executive summary, the seven dimension sections and the three contrary-evidence subsections was matched to its appendix row and then to the digest line(s) for that row's URL or publisher; memlog:// rows were checked against .memlog.md; six sources were spot-checked directly
---

# Citation check

## Summary

| Verdict | Count |
|---|---|
| SUPPORTED | 143 |
| OVERSTATED | 9 |
| UNSUPPORTED | 2 |
| MISATTRIBUTED | 3 |
| **Checked** | **157** |

Every number that carries a recommendation holds: the 969 sprite dereferences and the 30/86/52 callback sites (r4-verify), the -0.15 / 15% Fishtest pairing figure (redteam-sprt), the 780/700 and 750/1,220 SPRT run counts (sprt r1, r2), the ArchUnit 1.5.0 / 2026-08-04 line (archunit r1, Maven Central spot-check), the 4.0.0-beta = v3.3.8 commit and 291-commits-then-silence facts (spd r2), the AutoAscend 5,300 / 4,918 / 8,556 figures (nethack r1), and the BALROG 6.8% NetHack row (nethack r1, spot-checked verbatim; the red team's 40% is the MiniHack column). The two memlog-backed rows check out on content: [52]'s exception text matches .memlog.md line 20 verbatim; [63]'s run is logged at .memlog.md line 12 but the memlog does not record the result numbers (see below). The overturn in "Contrary evidence: the brain architecture" stands.

Nothing non-SUPPORTED changes a recommendation. The defects are: two figures attributed to pages that do not carry them, one citation pointing at the wrong Fishtest issue, one self-contradiction inside the SPD ecosystem digests (fork star counts), one "traced by the maintainers" that was in fact measured by the reporter, and a handful of precision losses in section 7 and contrary A.

## Non-SUPPORTED items

- [43] section 1 (What breaks): "Mockito-mocking `Gdx.gl`, which breaks once code needs real return values such as shader compile status" -> MISATTRIBUTED (minor): the shader-compilation caveat ("many things ... need more than just mocking GL20.class, like compiling shaders") is a participant comment on issue #3383, i.e. [42], per libgdx-headless-r2-1.md line 18; #5995 only says Mockito is bad practice and prefers a hidden window (r2 line 19). libgdx-headless-r3-1.md line 29 blends both issues into one claim. Cite [42] [43].
- [47] [46] section 1 (Versions and health): "1.14.0 (2025-10-19), 1.14.1 (2026-05-18), 1.14.2 (2026-06-05), confirmed by both the GitHub tags and the libgdx.com posts" -> OVERSTATED: the exact tag dates come from GitHub alone, with years "inferred from ordering and matched against libgdx.com news" (libgdx-headless-r2-1.md line 21); libgdx.com dates 1.14.0 to 2025-10-20 and carries one combined 1.14.1 & 1.14.2 post dated 2026-05-28 (r2 line 22; r3 line 9 of its Contradictions), so it corroborates month and order, not the day-level dates. The round-1 GitHub fetch also had inconsistent years (libgdx-headless-r1-1.md line 44). Say "GitHub tag dates, consistent with the libgdx.com posts".
- (no marker) section 1 (Confidence): "source read on master, matching the 1.14 line SPD uses" -> UNSUPPORTED (minor): no digest states which libGDX version Shattered Pixel Dungeon v3.3.8 depends on; "libGDX 1.14" appears only in the red-team brief's scope line (redteam-headless.md line 9), which is the run's own premise, and in the lead's reproduction (memlog line 20, harness-side, not SPD's build). Verify from the pinned build.gradle or drop the clause.
- [5] section 2 (Performance): "87 s for a 120-class project, was traced by the maintainers to pulling in large numbers of classpath classes" -> OVERSTATED: archunit-r1-1.md line 49 says the classpath-resolution reconciliation is "plausible ... not confirmed from the issue thread this run" and line 53 lists reading the comments as an open lead. Spot-check of the comments (below): maintainer codecholeric suggested checking imported-class count and resolution settings; the reporter (royteeuwen, not a maintainer) then measured 80 s -> 1.4 s with `resolveMissingDependenciesFromClassPath=false`, and codecholeric discussed the trade-offs. So the cause was confirmed in the thread, but by the reporter following a maintainer's suggestion. Say "traced in the issue thread to".
- [63] section 3 (Win rate as a Bernoulli SPRT): "the lead's Monte Carlo (4,000 trials per arm) reproduced 808 and 727 with error rates of 4.6% and 4.8%" -> UNSUPPORTED by the process record: .memlog.md line 12 logs the run (4000 trials/arm, seed 20260903) and says "results recorded in the research.md sprt section", and line 48 summarises it as "~700-800 runs one-sample ... Monte Carlo agree"; neither memlog line carries 808, 727, 4.6% or 4.8%, so the specific numbers exist only in the report and its appendix row and cannot be cross-checked against the memlog. Not disputed (they sit inside the 780/700 Wald and 748 Brownian bracket from sprt-fishtest-r2-1.md line 30), but the memlog should record the four numbers.
- [67] section 3 (Win rate as a Bernoulli SPRT): "A fixed-horizon test peeked at repeatedly inflates false positives to roughly a quarter" -> MISATTRIBUTED: sprt-fishtest-r1-1.md line 38 rates the ~26% figure medium because it was "seen only in secondary summaries this run", and the spot-check (below) confirms the cited page, evanmiller.org/sequential-ab-testing.html, contains no numeric peeking false-positive rate. The figure belongs to Miller's separate "How Not To Run an A/B Test" article, which was not fetched. Either fetch and cite that page or drop the number and keep the qualitative claim on [67].
- [88] section 6 (Bots, RL, and headless precedent): "the most-starred non-Evan mods have 27 stars or fewer and are mostly inactive" -> OVERSTATED: the same digest reports Experienced-Pixel-Dungeon-Redone at 83 stars (spd-ecosystem-r2-1.md line 39) and shpd-toolkit at 45 stars (spd-ecosystem-r1-1.md line 28); the "27 or fewer" figure (r2 line 42) is the top of one GitHub search for "shattered pixel dungeon mod" sorted by stars, which missed both. Say "the search's top hits had 27 stars or fewer; the largest known fork has 83".
- [106] [93] section 7 (Scale and layout): "forced to 0 below the full-UI minimum of 360 x 200 UI pixels" -> OVERSTATED (minor): noosa-ui-r2-1.md line 96 gives the condition as `min(Game.width/MIN_WIDTH_FULL, Game.height/MIN_HEIGHT_FULL) < 2*Game.density`, i.e. below twice the density-scaled minimum, not below 360 x 200 itself.
- [95] section 7 (Building blocks and reserved space): "sizes the `GameLog` to 160 UI pixels wide in full UI" -> OVERSTATED (minor): noosa-ui-r2-1.md line 51 says "160 minus tag width or inset".
- [109] section 7 (Forks) and Cross-dimension insights: "`GameScene.java` and `ui/` did not change between v3.3.7 and v3.3.8" / "GameScene and ui/ unchanged since v3.3.7" -> OVERSTATED: noosa-ui-r2-1.md line 101 records that v3.3.7 -> v3.3.8 changed `core/ui/changelist/v3_X_Changes.java` (+25/-21) inside `ui/`; only GameScene.java itself was untouched. Say "GameScene.java and ui/ apart from the changelog class".
- (no marker) section 7 (Question paragraph): "a first pass on upstream `master` (post-beta) found the same code" -> OVERSTATED (minor): the upstream reader's verdict table marks the master read as "VERIFIED in substance; DIFFERS in construction order" for the HUD (noosa-ui-r2-1.md line 20) and notes that master is not in the local clone so any master difference "cannot be measured here" (line 102). The memlog (line 61) says the same: "match the master read with order corrected". Say "found the same code in substance, with the HUD construction order corrected at the tag".
- [97] [126] contrary evidence A (The actor wait): "`isMoving` is cleared solely in `onComplete(Tweener)` from the render thread" -> OVERSTATED (minor): libgdx-headless-r4-verify.md line 19 (Answer) and the finding at its line 26 list two other releases, thread interruption (`Actor.java:283-284`) and `TenguSprite`, which clears the flag in its own `play()` and `onComplete(Animation)` overrides. All are still render-thread or exceptional paths, so the conclusion holds; "solely" does not. (Related, sub-threshold: "which only `CharSprite.onComplete(Animation)` calls" in the first bullet omits the five sprite subclasses that call `onAttackComplete()` directly, r4-verify line 33 and line 37; those are also animation callbacks, so the bullet's substance stands.)
- [113] [114] contrary evidence B (Small samples): "GSPRT's guarantees are asymptotic and Fishtest exercises them at about 100,000 games per test; sequential t-statistics are known to be inaccurate at small sample sizes" -> MISATTRIBUTED: the asymptotic-guarantee and ~10^5-games points come from the Fishtest Mathematics wiki, appendix [53] (redteam-sprt.md line 23); the small-sample point is [113] (line 21). [114] is fishtest issue #865 (bounds V2, line 25) and supports neither half of this sentence; it belongs to the next bullet, where it is already cited. Change to [53] [113].
- [115] contrary evidence B (Small samples): "One simulation study found an SPRT reaching 73% power against 83% for a fixed-sample design when the true effect differed from the hypothesized one" -> OVERSTATED (minor): redteam-sprt.md line 27 reports the 73% vs 83% result "under identical conditions" and states the dependence on the true effect matching the hypothesised alternative as a separate, general point; the digest does not say the 73% run used a misspecified alternative. Drop the "when the true effect differed" qualifier or attribute it as the skeptic's interpretation.

## Notes outside the count

- Executive summary and section 5 [28], and contrary evidence C: "every LLM below 7% NetHack progression" / "the Gemini-3-Pro row reads MiniHack 40.0 ± 7.7, NetHack 6.8 ± 3.2" -> SUPPORTED, but flagged here because the two digests disagree (nethack-autoascend-r1-1.md line 30 says 6.8%; redteam-brain.md line 13 says 40.0%) and my first summarised fetch of balrogai.com repeated the red team's error. A verbatim transcription of the row (spot-check 2) gives nine cells against nine headers: `Gemini-3-Pro | 58.1 | 96.0 | 57.3 | 60.2 | 88.3 | 40.0 | 6.8 | 2026-02-03` under `Agent | % Progress | BabyAI | Crafter | TextWorld | BabaIsAI | MiniHack | NetHack | Date`, so NetHack = 6.8 and MiniHack = 40.0. The Claude-Opus-4.5-Thinking (30.0) and Gemini-3.1-Pro-Thinking (27.5) figures the red team quoted are likewise MiniHack; their NetHack values are 2.4 and 2.6. The report's reading and the memlog (line 62) are correct; the red team mis-aligned columns, and so does a summarising fetch of the page.
- [117] contrary evidence A: the JNI one-loader rule is not in any digest (redteam-headless.md line 31 says the spec text was not fetched); the memlog (line 59) records the lead reading it. Spot-check 1 confirms the verbatim sentence, so it is counted SUPPORTED.
- [2] section 2: "Maven Central confirms 1.5.0 as the latest `archunit-junit5` artifact, uploaded the same day" is not in any digest (archunit-r1-1.md line 16 could only reach the stale legacy index); the memlog (line 13) records the lead's independent check. Spot-check 3 confirms `<latest>1.5.0</latest>`, `<lastUpdated>20260804005331</lastUpdated>`, so it is counted SUPPORTED.
- [48] section 1: "LWJGL 3.3.3 has been pinned since 1.13.1" was rated low-medium in libgdx-headless-r3-1.md line 20 because the fetch summariser attached "Update to 3.4.3" to 1.14.0, 1.14.2 and 1.14.3 at once. Spot-check 6 places that line under the unreleased [1.14.3] header and "Update to LWJGL 3.3.3" under [1.13.1], so the report's statement is correct and counted SUPPORTED; the section's "high" confidence is now earned rather than assumed.
- Run header: "Twelve research assistants and three skeptics" -> the digests directory holds fifteen assistant digests (seven r1, six r2, one r3, one r4) of which two are the upstream-reader agent, plus three red-team digests. Process description, not a sourced claim; not counted.
- Frontmatter tally (36 verified / 6 unverified / 1 overturned) matches .memlog.md line 72.

## Spot-checks performed (6 sources; 7 fetches, one blocked and retried by another route)

1. https://docs.oracle.com/en/java/javase/21/docs/specs/jni/invocation.html for [117]: page contains verbatim "The same JNI native library cannot be loaded into more than one class loader. Doing so causes UnsatisfiedLinkError to be thrown." Confirms the report.
2. https://balrogai.com/ for [28] (fetched twice: a summarised read returned NetHack 40.0, a verbatim row transcription returned the nine-cell row above with NetHack 6.8). Confirms the report and the memlog; the summarised read and the red-team digest both mis-aligned columns.
3. https://repo1.maven.org/maven2/com/tngtech/archunit/archunit-junit5/maven-metadata.xml for [2]: WebFetch got HTTP 403; curl returned `<latest>1.5.0</latest>`, versions ending 1.4.0, 1.4.1, 1.4.2, 1.5.0, `<lastUpdated>20260804005331</lastUpdated>`. Confirms the report.
4. https://api.github.com/repos/TNG/ArchUnit/issues/1160/comments for [5]: five comments; the reporter measured 80 s -> 1.4 s after setting `resolveMissingDependenciesFromClassPath=false` following a maintainer (codecholeric) suggestion; maintainer then discussed `maxIterations*` alternatives. Confirms the cause; "traced by the maintainers" is overstated as recorded above.
5. https://www.evanmiller.org/sequential-ab-testing.html for [67]: the page discusses peeking qualitatively and gives no numeric false-positive rate. The "roughly a quarter" figure is not on the cited page, as recorded above.
6. https://raw.githubusercontent.com/libgdx/libgdx/master/CHANGES for [48]: "Update to LWJGL 3.3.3" is under [1.13.1]; "LWJGL3: Update to 3.4.3" is under [1.14.3] (unreleased); "libGDX is now built using Java 17 due to Gradle 8 requirements" is under [1.13.0]. Confirms the report and resolves the round-3 digest's ambiguity.
