---
scope: Shatterfish v1 (E1 to E5) with E6 interfaces reserved
purpose: build-substrate
altitude: initiative
updated: 2026-09-04T00:17
---

- (constraint) Non-negotiables 1-8 bind every AD (CLAUDE.md); bootstrap section 4 is the starting position; PRD v3 FR-1..FR-53 and NFR-1..NFR-6 are inputs; docs/codebase-map.md findings are Tier 1 facts
- (constraint) ADR-0003 (module layout, Java 21, brain boundary enforced three ways) is adopted as-is; the bootstrap item 'module boundaries and the ArchUnit rule' needs no new ADR
- (direction) Fast path per the owner's standing delegation; hard decisions (Observer, RNG) get a fairness-reviewer red-team pass before the PR
- (direction) Session 11 writes the spine draft and ADR-0005..0008 (Observation schema and hashing, Observer visibility rules, RNG seeding, hook guarding); session 12 writes ADR-0009..0013 (snapshot/redetermination, search deferral criteria, run-log format, rig statistics, overlay threading), runs the reviewer gate, finalizes, and presents Checkpoint D
- (decision) AD-2/ADR-0005: Observation = record tree in api, whitelist by construction, no secret enum members, no floats; hand-written canonical binary codec + SHA-256 over section hashes; hand-written JSON only for logs. Binds FR-3/8/9/23; prevents two serializations and default leaks. Rejected: Map blob, screenshot, stripped Bundle, Java serialization, Jackson/Gson, protobuf, raw Terrain ints
- (decision) AD-3/ADR-0006: Observer reads only the renderer's and HUD's predicates (heroFOV/visited/mapped/discoverable, tile-sheet mapping for secrets, Trap.visible, Heap.seen with container opacity, sprite.visible incl. invisible flag and mimic quirk, icon-only buffs, hunger 3-state, item visibly* accessors, getKnown, blobs masked by FOV, GameLog lines, Notes, open Window as Prompt); never CellSelector hit test, Level.mobs outside FOV, unknown classes, ID counters, Catalog, exact hunger. Runs at an Input wait on the UI-role thread. OracleObserver separate, refused by the Rig, excluded from the hash. Rejected: scene-graph scraping, pixel parsing, draw-call hooks, blacklist, own shadowcasting
- (decision) AD-6/ADR-0007: seed = the game's seed (customSeedText set, so a seeded game); reseed base generator from mix(seed,k) at every Input wait after Dungeon.init; fresh standard Profile per Run (intro off, all Document pages read, no bones/badges/rankings); hooks: Actor.all/chars LinkedHashSet, Random.chances(HashMap) sorted class keys; Emitter/Music routing reserved for E5 if the Replay test needs it; determinism test in two JVMs. Rejected: seed before init, replace Random, per-actor generators, 64-bit seed hashed down, deterministic Actor.hashCode, hooking guide-page generator
- (decision) AD-10/ADR-0008: one registry class Hooks.java added to core (nullable static listeners, interfaces declared there); each site one line with marker comment shatterfish-hook:<id>; vanilla branch when nothing registered; HooksTest counts markers vs docs/UPSTREAM.md rows and enforces the budget of eight; accessors instead of visibility changes; expected ledger of eight rows. Rejected: hand-tracked plain edits, bytecode instrumentation, patch files, classpath shadowing, reflection
- (decision) Paradigm: ports-and-adapters around one immutable Observation; Observer and ActionExecutor the only ports; Brain a pure function (Observation, Belief) -> (Decision, Belief); drivers are adapters (headless, embedded, replay); the Input wait index k is the primary key across Observation, Decision, Action, Run-log record and RNG reseed (AD-5)
- (decision) AD-8 threads: game actor thread untouched; UI-role thread (render in Overlay, driver headless) observes, executes Actions and writes the Panel; brain worker holds only the immutable Observation; thread assertions on every port entry. Detail deferred to ADR-0013
- (assumption) Mob HP is exposed as the health bar's fraction; the alert emote is the only alert signal; both Tier 3 until the E1 Observer story reads CharHealthIndicator and EmoIcon
- (question) Does per-Input-wait reseeding interact with any generator the game pushes across an Input wait? Fairness red-team pass requested on ADR-0005..0007 before the PR
- (event) Spine draft written with AD-1..AD-12; ADR-0005..0008 written as proposed; ADR-0009..0013 deferred to session 12
- (event) fairness-reviewer red team on ADR-0005..0007: 21 findings (6 blocking), all accepted
- (decision) Harness RNG stream derives from a per-Run salt drawn by the runner, logged, never observed; the seed-derived stream was rejected because the seed is on the HUD and in published Seed sets, so a Brain could predict every roll with no game imports. Run tuple gains the salt. brain additionally forbidden java.io/nio.file/net/reflect by ArchUnit (no channel but the Observation)
- (decision) Observer corrections from the red team: traps gated by Fog != UNKNOWN (painter pre-reveals, trigger reveals anywhere); neutral passive mimics emitted as CHEST heaps with a chest-vs-mimic differential test; canonical list orders in the codec (cell index, class name, Belongings order) with a shuffle test; health quantised to the bar's pixel width; emote read from CharSprite.emo via accessor (SLEEP/ALERT/LOST/INVESTIGATE); all iconed buffs with turns for hero and mobs, no cap; blobs as kind sets without volume; log from the GLog.update signal not GameLog entries; Input wait = hero ready and (no window or a Prompt window); heaps report current top item, hidden flag, crystal-chest category, single-item price; seed and turn leave the Observation (turn to the Run log as fixed-point); oracle data becomes a sidecar with header.oracle hashed
- (decision) RNG corrections: identity-order hook row now covers Level.mobs, Level.blobs, Random.element too (Actor.init re-inserts from them per level); Emitter/Music/EmoIcon routing hook is required for E5 Replay (EmoIcon draws on the actor thread); Profile pins language=en and is versioned into the Run tuple; hidden-door consequence reworded (tutorial forcing off, normal roll applies); hero.live()/initHero after resetGenerators verified in E1 or a one-line hook after Dungeon.java:254
- (decision) Hook ledger reshuffled: 3 scene seam + Input-wait notification; 4 read-only accessors (GameScene HUD fields, cellSelector, CharSprite.emo); 5 Hero.ready/interrupt guards; 6 identity-order removal; 7 sprite-wait bypass; 8 render-thread draw routing. A row may cover several one-line sites of the same kind under one id
- (question) AD-3 still says 'hero ready, no Window'; align with AD-5's Prompt-window wording at session 12 finalize
- (decision) ADR-0009: Snapshot = the game's own bundles as bytes + (salt, k, profile, tag); restore through loadGame/loadLevel/switchLevel in the Run's Profile; restore-and-replay test from E1; redetermination = bundle rewrite by a SnapshotScrubber per a hidden-element table (unknown item classes relabelled, unseen mobs removed/resampled, UNKNOWN cells made WALL, secret door/trap scrubbed, container contents redrawn, decks reset, salt from the sample); belief-built world kept as E6 alternative; rollout host swap-in-place baseline, classloader isolation alternative; process pool rejected (#4). Rejected: deep copy, replay-from-start as mechanism, CRIU, parallel world model, in-memory mutation
- (decision) ADR-0010: E4 = policies + safeTest; E6 starts with one-ply expectimax over Codex tables (fights); then measure simulator speed, Long et al. properties and the FR-13 search leak test; choice rule: leak test must pass, budget fit per Registration, then the Rig decides candidate vs one-ply at equal wall-clock budget; Long et al. explanatory only. No numeric threshold in the ADR
- (decision) ADR-0011: gzip JSONL per Run (<tag>-<class>-<challenges>-<seedcode>-<salt>.jsonl.gz), records header/wait/prompt/mode/unsupported/end, canonical JSON from the api JsonWriter (sorted keys, integers only, scores in ten-thousandths), chain_k = SHA-256(chain_{k-1} || canonical(record minus prev/chain/think_ms)), Replay compares obs hashes per k and names the section on mismatch, verification = recompute chain + Replay. Rejected: SQLite, protobuf, CSV, single JSON, Bundle, plain text
- (decision) ADR-0012: Per-pair statistic = lexicographic Composite comparison on (seed, salt) pairs scored 1/0.5/0; GSPRT ported from Fishtest sprt.py (approximation 2.1, regularized, clamped) with p0, p1, alpha, beta, n0, nmax all Registration fields set by the E3 calibration story (no defaults in the ADR); e-process implemented side by side as the calibrated alternative; reported: LLR trace, pair mean CI, measured pair correlation, Wilson win rate, survival curve, boss staircase, fixed-sample cross-check. Rejected: Score/win-rate/depth differences as the gate, Bayesian, pentanomial
- (decision) ADR-0013: three roles (game actor thread untouched; UI-role thread = render thread in Overlay / driver thread headless, the only thread that observes, executes, writes Panel and log; single brain worker returning a future polled per frame); Input-wait detection via the Hero.ready first-call notification (hook row 3) + AD-5 check; PAUSED drops cell input via the Panel's own CellSelector listener and deactivates toolbar/inventory pane through the accessor row; HUMAN records actions from Hero.curAction and an Item.execute notification (hook row 3 second site) and calls Brain.update; budget overrun = THINKING label, never a wait or cancel; no monitors taken on game objects. Rejected: brain on render/actor thread, extra driver thread in Overlay, worker posting to scene, blocking with timeout
- (decision) Spine: AD-3 aligned to AD-5's Prompt-window wording; AD-7 gains Brain.update for human turns and k-tagged Decisions; AD-9 names the api types and the scrubbed-flag assertion; AD-11 lists the Registration fields; Deferred trimmed to E3/E6/E1-spike items plus the operational envelope row
- (event) Reviewer gate run: 6 parallel reviewers (rubric, versions, adversary, PRD reconcile, UX+codebase reconcile, fairness red team). Verdict BLOCK: 2 critical, 7 fairness-blocking, 18 composition clashes, ~100 findings total. Reports in reviews/
- (decision) ADR-0014 (new): sealed Action interface in api, one record per kind, every parameter a value the Observation carries; ONE STEP PER ACTION for movement because Hero.actMove keeps curAction and never returns to ready between cells (so a multi-cell target would make per-cell Decisions and interruption impossible); targeting drives the game's own selector inside the same wait; ItemRef = Belongings order + display name; Wait invalid at a Prompt
- (decision) ADR-0015 (new): driver thread owns the headless loop (HeadlessApplication only for the Gdx statics), harness-owned HeadlessScene with a no-op GL20 and Pixmap atlases, sprites and EmoIcons created as the game does; Input-wait detection moves to the Dungeon.observe() site inside Hero.act()'s !ready branch (Hero.java:840-848), NOT Hero.ready() which fires ~60x/s; scene-seam hook also fires on scene creation so the Observer re-registers its GLog listener
- (change) Gate fixes applied: k out of the hashed header; sealed/Level.locked and Codex version into the header; Belief is an opaque api value so harness can hash it without depending on brain (AD-13); Snapshot is a harness type with only an opaque handle in api (a Brain could inflate the bundle bytes with the JDK alone); mix(salt,k) defined exactly with the SplitMix64 finalizer and a published test vector; salts drawn at execution time, never pre-committed; hook budget 8 -> 10 with an input-gate row and an SPDAction row (PRD section 10 amended); Run id gains the Brain; run log is plain .jsonl (NFR-9); AD-14 one-owner rule added; conventions gain Network (NFR-8) and Portability (NFR-7); Config allows Playbooks and weights; holdout scoped to release claims; pairing is per triple with a missing-pair rule
- (change) Second gate pass: ADR-0010 drops 'bias' from the E6 measurement (Long et al. define it as the game favouring one player over the other, meaningless single-player; leaf correlation and disambiguation transfer) and routes search through the api Simulator with an opaque handle; ADR-0011 writes plain .jsonl (NFR-9), run id gains the Brain, header gains the Codex version, records gain shadow and boundary kinds, wait gains highlights, incomplete Runs defined; ADR-0009 rewritten (Snapshot is harness-only, seed scrubbed, visible mobs' AI state and exact HP scrubbed, mimic row, restore contract for Generator.fullReset draws); ADR-0013 rewritten (detection deferred to ADR-0015, PAUSED input gate is a hook, RUN OVER via gameOver/WndResurrect, HUMAN runs update plus a shadow decide, human-action recording sites enumerated, save/resume boundary)
- (decision) PRD v4: hook budget raised from eight to ten with a NOTE FOR PM, because the input gate (FR-40) and the SPDAction registration point (FR-42, FR-45) cannot be avoided; the ledger now lives in ADR-0008 and is counted by a test
- (event) spine finalized
