# Filled in by :codex:processResources from the root build script, which is where upstream keeps
# its version (build.gradle: appVersionName). The Codex folder is named by the tag, v and this.
version.name=${versionName}
