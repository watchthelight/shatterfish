# Filled in by :harness:processResources from the root build script, which is where upstream
# keeps its version (build.gradle: appVersionName, appVersionCode). The desktop launcher reads
# the same two values from its jar manifest; a headless process has no manifest to read.
version.name=${versionName}
version.code=${versionCode}
